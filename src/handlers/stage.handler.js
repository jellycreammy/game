export const moveStageHandler = (payload) => {
    const { currentStage, targetStage } = payload;
    // 스테이지 이동 로직
    console.log(`Moving from stage ${currentStage} to stage ${targetStage}`);
    // 스테이지 이동 관련 로직 추가
  };
  